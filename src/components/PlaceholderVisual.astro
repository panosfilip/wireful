---
import type { CoverMotif } from "../data/caseStudies";

interface Props {
  motif: CoverMotif;
  alt: string;
}

const { alt } = Astro.props;
---

<img src="media/work-placeholder.svg" alt={alt} />
