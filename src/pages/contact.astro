---
import Base from "../layouts/Base.astro";
import { site } from "../data/site";
import { withPlaceholders } from "../utils/placeholders";
---

<Base title={`Contact | ${site.name}`}>
  <section class="hero hero-inner contact-cta">
    <div class="container">
      <h1 data-intro style="--intro: 1">Tell me what you want to <span class="title-keyword">improve</span>.</h1>
      <p class="lead" data-intro style="--intro: 2">
        I work on new websites, WordPress redesigns, Figma prototypes, brand systems, campaign
        assets, and product UI. Send a short outline and I will reply with the next practical
        step. Hiring enquiries are welcome too.
      </p>

      <a class="mail-link" href={site.emailHref} data-intro style="--intro: 3">
        {site.email}
      </a>

      <div class="contact-lines" data-reveal-group>
        {
          site.socials.map((s) => (
            <a class="contact-line" href={s.href}>
              <span class="label">{s.label}</span>
              <span set:html={withPlaceholders(s.note)} />
            </a>
          ))
        }
        <div class="contact-line">
          <span class="label">Location</span>
          <span>{site.location}</span>
        </div>
      </div>
    </div>
  </section>
</Base>
