/**
 * Browser-chrome theme colors for the <meta name="theme-color"> tag.
 * Keep in sync with the --bg values in global.css for each theme.
 */
export const themeColors = {
  light: "#f6f1f7",
  dark: "#0b0712",
} as const;
